export function homeNavX(window: Window): number {
  return window.innerWidth / 2 - 140; // MAGIC
}

export function homeNavY(window: Window): number {
  return window.innerHeight / 2 + 160; // MAGIC
}
