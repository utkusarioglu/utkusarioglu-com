// import './Header.view.scss';

const HeaderView = () => {
  return (
    <header {...{ className: 'header' }}>
      <div {...{ className: 'header__title' }}>
        <h1>Utku Sarioglu</h1>
      </div>
    </header>
  );
};

export default HeaderView;
